openapi: 3.2.0
info:
  title: HackMD Open Team Folders API
  version: 1.0.0
  description: HackMD OpenAPI
  license:
    name: UNLICENSED
  contact:
    name: HackMD Team
    email: support@hackmd.io
servers:
- url: /v1
  description: API Server
tags:
- name: Team Folders
  description: Operations related to folders within a team.
paths:
  /teams/{teampath}/folders:
    get:
      operationId: ListTeamFolders
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApiFolder'
                type: array
      description: List all folders in a team workspace
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - description: The path identifier for the team.
        in: path
        name: teampath
        required: true
        schema:
          type: string
      summary: List team folders
      x-summary-source: derived
    post:
      operationId: CreateTeamFolder
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: {}
      description: Create a new folder in a team workspace
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - description: The path identifier for the team.
        in: path
        name: teampath
        required: true
        schema:
          type: string
      requestBody:
        description: The folder properties.
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeamFolderBody'
              description: The folder properties.
      summary: Create team folder
      x-summary-source: derived
  /teams/{teampath}/folders/folder-order:
    get:
      operationId: GetTeamFolderOrder
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiFolderOrder'
      description: 'Get your personal folder ordering for this team workspace (parent folder id or `root` → ordered child folder ids).

        Uses the same folder UUIDs as (not internal Yjs clientIds).'
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - in: path
        name: teampath
        required: true
        schema:
          type: string
      summary: Get team folder order
      x-summary-source: derived
    put:
      operationId: UpdateTeamFolderOrder
      responses:
        '204':
          description: No content
          content:
            application/json:
              schema: {}
      description: Replace your personal folder ordering for this team workspace.
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - in: path
        name: teampath
        required: true
        schema:
          type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFolderOrderBody'
      summary: Update team folder order
      x-summary-source: derived
  /teams/{teampath}/folders/{folderId}:
    get:
      operationId: GetTeamFolder
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                anyOf:
                - $ref: '#/components/schemas/ApiFolder'
                - {}
      description: Get a single folder in a team workspace
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - description: The path identifier for the team.
        in: path
        name: teampath
        required: true
        schema:
          type: string
      - description: The ID of the folder to retrieve.
        in: path
        name: folderId
        required: true
        schema:
          type: string
      summary: Get team folder
      x-summary-source: derived
    patch:
      operationId: UpdateTeamFolder
      responses:
        '202':
          description: Accepted
          content:
            application/json:
              schema: {}
      description: Update a folder in a team workspace
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - description: The path identifier for the team.
        in: path
        name: teampath
        required: true
        schema:
          type: string
      - description: The ID of the folder to update.
        in: path
        name: folderId
        required: true
        schema:
          type: string
      requestBody:
        description: The properties to update.
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTeamFolderBody'
              description: The properties to update.
      summary: Update team folder
      x-summary-source: derived
    delete:
      operationId: DeleteTeamFolder
      responses:
        '204':
          description: No content
      description: Delete a folder in a team workspace
      tags:
      - Team Folders
      security:
      - token: []
      parameters:
      - description: The path identifier for the team.
        in: path
        name: teampath
        required: true
        schema:
          type: string
      - description: The ID of the folder to delete.
        in: path
        name: folderId
        required: true
        schema:
          type: string
      summary: Delete team folder
      x-summary-source: derived
components:
  schemas:
    FolderName:
      type: string
      example: Engineering
      description: Folder name.
    UpdateFolderOrderBody:
      properties:
        order:
          $ref: '#/components/schemas/ApiFolderOrder'
      required:
      - order
      type: object
      additionalProperties: false
    ApiFolder:
      properties:
        id:
          type: string
        name:
          $ref: '#/components/schemas/FolderName'
        description:
          type:
          - string
          - 'null'
        icon:
          allOf:
          - $ref: '#/components/schemas/FolderIconCodepoint'
        color:
          allOf:
          - $ref: '#/components/schemas/FolderColorHex'
        parentFolderId:
          type:
          - string
          - 'null'
        createdAt:
          type: number
          format: double
        updatedAt:
          type: number
          format: double
      required:
      - id
      - name
      - description
      - icon
      - color
      - parentFolderId
      - createdAt
      - updatedAt
      type: object
      additionalProperties: false
    CreateTeamFolderBody:
      properties:
        color:
          $ref: '#/components/schemas/FolderColorHex'
        icon:
          $ref: '#/components/schemas/FolderIconCodepoint'
        description:
          type: string
        parentFolderId:
          type: string
        name:
          $ref: '#/components/schemas/FolderName'
      type: object
    FolderIconCodepoint:
      type: string
      example: 1F525
      description: 'Folder icon as an emoji unified codepoint string.

        Examples: `1F525` (🔥), `2764-FE0F` (❤️).'
    ApiFolderOrder:
      description: 'Per-user display order of folders: maps each parent folder id (or `root`) to

        an ordered list of child folder ids. Keys and values use the same UUIDs as

        {@link ApiFolder.id} (not Yjs clientIds stored internally in preferences).'
      properties: {}
      type: object
      additionalProperties:
        items:
          type: string
        type: array
    UpdateTeamFolderBody:
      properties:
        parentFolderId:
          type:
          - string
          - 'null'
        color:
          allOf:
          - $ref: '#/components/schemas/FolderColorHex'
        icon:
          allOf:
          - $ref: '#/components/schemas/FolderIconCodepoint'
        description:
          type:
          - string
          - 'null'
        name:
          $ref: '#/components/schemas/FolderName'
      type: object
    FolderColorHex:
      type: string
      example: '#FF6B6B'
      description: 'Folder display color in hexadecimal format.

        Accepts `#RGB`, `#RGBA`, `#RRGGBB`, or `#RRGGBBAA`.'
  securitySchemes:
    token:
      type: http
      scheme: bearer
